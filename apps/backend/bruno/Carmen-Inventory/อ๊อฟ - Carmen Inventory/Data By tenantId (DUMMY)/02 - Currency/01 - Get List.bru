meta {
  name: 01 - Get List
  type: http
  seq: 1
}

get {
  url: {{host}}/api/v1/currencies?sort=code,name:asc
  body: none
  auth: bearer
}

params:query {
  sort: code,name:asc
  ~page: 1
  ~filter[isActive: bool]: 1
  ~searchfields: code,name,description,isActive:bool
  ~filter[code]: us
  ~search: baht
  ~advance: {'where':{'code':'usd'}}
  ~perpage: 2
}

headers {
  x-tenant-id: {{tenantId}}
}

auth:bearer {
  token: {{access_token}}
}
