meta {
  name: 01 - Get List
  type: http
  seq: 1
}

get {
  url: {{host}}/api/v1/currencies?advance={'where':{'code':'usd'}}&page=1&sort=code:desc, name:asc&search=aaa
  body: none
  auth: bearer
}

params:query {
  advance: {'where':{'code':'usd'}}
  page: 1
  sort: code:desc, name:asc
  search: aaa
  ~searchfields: code
  ~filter[code]: th
  ~perpage: 2
  ~filter[symbol]: 
}

headers {
  x-tenant-id: {{tenantId}}
}

auth:bearer {
  token: {{access_token}}
}
