meta {
  name: 01 - Get List
  type: http
  seq: 1
}

get {
  url: {{host}}/api/v1/currencies?page=1&filter[isActive:bool]=false
  body: none
  auth: bearer
}

params:query {
  page: 1
  filter[isActive:bool]: false
  ~filter[code]: us
  ~sort: code:desc,name:asc
  ~search: baht
  ~advance: {'where':{'code':'usd'}}
  ~searchfields: code,name,description,isActive:bool
  ~perpage: 2
}

headers {
  x-tenant-id: {{tenantId}}
}

auth:bearer {
  token: {{access_token}}
}
